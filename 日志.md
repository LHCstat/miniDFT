# 8.25
1.由于io.py和python自有的包重名了，所以把io.py改成了i_o.py
--
2.hamiltonian.py 两个函数研究问题，其次系数还没给
--
3.eigensolver.py 研究还没详细写
--
4.fft_grid.py 函数还没写
--
5.还有一个最大问题，现在把c当一个列向量存储，但是其实每一个元素都是一个三维向量，怎么搞
--
6.还需要设计得到G点个数的函数
# 8.26
--
1.放行向量还是列向量没确定(np数组遍历默认每一行遍历)
2.k还没纳入考虑
3.直接默认了每个状态填充两个电子，忽略了奇数电子情况