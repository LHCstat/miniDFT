可以。我们现在把它从“模块说明”提升到**整个程序运行时的数据流设计**。你以后写代码时，最重要的就是按照这张数据流图走。

我会以我们之前确定的架构为准，不重新换架构。

---

# 一、先看完整总数据流

假设用户最终这样运行：

```bash
python main.py input.yaml
```

那么整个程序的数据流是：

```text
┌──────────────────────┐
│     input.yaml       │
│                      │
│ lattice              │
│ atoms                │
│ electron_number      │
│ ENCUT                │
│ k-points             │
│ potential parameters │
│ SCF parameters       │
│ DOS parameters       │
└──────────┬───────────┘
           │
           ↓
       ┌────────┐
       │ io.py  │
       └───┬────┘
           │
           ↓
       ┌──────────┐
       │ system.py│
       └────┬─────┘
            │
       ┌────┴─────────────┐
       ↓                  ↓
  lattice.py          potential
       │                  │
       ↓                  ↓
  实空间/倒空间       ionic_potential.py
  晶格参数               │
       │                 │
       └────────┬────────┘
                ↓
             basis.py
                │
                ↓
          PlaneWaveBasis
                │
                ↓
           fft_grid.py
                │
                ↓
          FFTGrid / 映射
                │
                ↓
       ┌────────────────┐
       │ initial_density│
       └───────┬────────┘
               ↓
          ┌─────────┐
          │  SCF    │
          │ scf.py  │
          └────┬────┘
               │
       ┌───────┴────────┐
       ↓                ↓
   density.py       potentials
       │                │
       │       ┌────────┼─────────┐
       │       ↓        ↓         ↓
       │   ionic.py  hartree.py  xc.py
       │       │        │         │
       │       └────────┼─────────┘
       │                ↓
       │         potentials.py
       │                │
       │                ↓
       │         V_eff(r)
       │                │
       │                ↓
       │        hamiltonian.py
       │                │
       │                ↓
       │             H(C)
       │                │
       │                ↓
       │        eigensolver.py
       │                │
       │                ↓
       │          ε_nk , C_nk
       │                │
       │        ┌───────┴────────┐
       │        ↓                ↓
       │ occupations.py   wavefunction.py
       │                         │
       └──────────────┬──────────┘
                      ↓
                 density.py
                      │
                      ↓
                 n_new(r)
                      │
                      ↓
                 mixing.py
                      │
                      ↓
                 n(r) ←─────────┐
                      │         │
                      └─────────┘
                         SCF循环
                           │
                           ↓
                      convergence
                           │
                           ↓
                   ┌───────┴────────┐
                   ↓                ↓
               energy.py         dos.py
                   │                │
                   ↓                ↓
             total energy       DOS(E)
                   │
                   └───────┬────────┘
                           ↓
                         io.py
                           ↓
                    最终输出文件
```

这张图就是整个 Mini-DFT 的“程序地图”。

下面把它拆开。

---

# 二、第一阶段：输入文件

假设我们设计一个：

```text
input.yaml
```

第一版可以长这样：

```yaml
system:
  lattice:
    a1: [5.0, 0.0, 0.0]
    a2: [0.0, 5.0, 0.0]
    a3: [0.0, 0.0, 5.0]

  atoms:
    - element: X
      position: [0.0, 0.0, 0.0]

  electron_number: 2

basis:
  encut: 20.0

kpoints:
  mode: gamma
  points:
    - [0.0, 0.0, 0.0]
  weights:
    - 1.0

potential:
  type: cosine
  V0: -1.0

scf:
  max_iterations: 100
  density_mixing: 0.3
  tolerance: 1e-6

dos:
  emin: -10.0
  emax: 10.0
  points: 2000
  sigma: 0.05
```

这里要特别注意：

**输入文件只描述“我要算什么”。**

它不应该描述：

```text
怎么 FFT
怎么求 Hartree
怎么求本征值
怎么迭代
```

这些属于程序内部。

---

# 三、`main.py`：启动程序

用户：

```bash
python main.py input.yaml
```

进入：

```python
main.py
```

它首先调用：

```python
config = io.read_input("input.yaml")
```

得到一个配置对象：

```text
config
├── lattice
├── atoms
├── electron_number
├── encut
├── kpoints
├── potential
├── scf
└── dos
```

然后：

```python
system = System(...)
```

---

# 四、`io.py → system.py`

这里发生第一次真正的数据结构转换：

```text
input.yaml
     ↓
    io.py
     ↓
Python configuration
     ↓
  system.py
     ↓
System object
```

`System` 里面保存：

[
\mathbf a_1,\mathbf a_2,\mathbf a_3
]

原子位置：

[
\mathbf R_I
]

电子数：

[
N_e
]

k 点：

[
\mathbf k
]

k 点权重：

[
w_k
]

以及：

[
E_{\rm cut}.
]

---

# 五、`system.py → lattice.py`

接下来：

```python
lattice = Lattice(
    system.lattice_vectors
)
```

`lattice.py` 计算：

[
A
=

(\mathbf a_1,\mathbf a_2,\mathbf a_3)
]

以及：

[
B=2\pi(A^{-1})^T.
]

得到：

[
\mathbf b_1,\mathbf b_2,\mathbf b_3.
]

同时计算：

[
\Omega=|\det A|.
]

因此：

```text
System
 │
 └── lattice vectors
          ↓
     lattice.py
          ↓
 ┌─────────────────┐
 │ A               │
 │ B               │
 │ volume          │
 └─────────────────┘
```

---

# 六、`lattice.py → basis.py`

现在进入平面波基组。

输入：

```text
lattice
k-points
ENCUT
```

进入：

```python
PlaneWaveBasis(...)
```

---

## 6.1 枚举 G

生成：

[
\mathbf G
=========

h\mathbf b_1+k\mathbf b_2+l\mathbf b_3.
]

然后筛选：

[
\boxed{
\frac12|\mathbf k+\mathbf G|^2
\le E_{\rm cut}
}
]

最终得到：

```text
G_vectors
```

例如：

```text
G[0]
G[1]
G[2]
...
G[Npw-1]
```

所以这里产生一个非常重要的数据：

[
\boxed{N_{\rm PW}}
]

即平面波数量。

---

# 七、这里回答你之前的一个核心问题

你之前问：

> “平面波是通过 G 的截断确定的吗？能不能直接给 ENCUT？”

现在整个程序的数据流可以非常明确地看到：

```text
用户
 ↓
ENCUT
 ↓
basis.py
 ↓
生成 G
 ↓
筛选 |k+G|²/2 <= ENCUT
 ↓
Npw
```

所以：

[
\boxed{
E_{\rm cut}
\text{ 是用户输入}
}
]

而：

[
\boxed{
{\mathbf G},N_{\rm PW}
\text{ 是程序计算出来的}
}
]

这就是我们采用的设计。

---

# 八、`basis.py → fft_grid.py`

有了：

[
\mathbf G
]

接下来建立 FFT 网格。

```python
fft_grid = FFTGrid(
    lattice=lattice,
    basis=basis
)
```

它建立：

```text
G
↓
FFT index
```

的映射。

比如：

[
G=(h,k,l)
]

对应：

```text
fft_array[i,j,k]
```

中的位置。

---

# 九、为什么现在还没有波函数？

因为此时我们只是建立了：

[
\boxed{\text{“波函数可以由哪些平面波组成”}}
]

还没有得到：

[
C_{n\mathbf k}(G).
]

这点非常重要。

目前我们只有：

```text
basis
├── G
├── k+G
├── kinetic energy
└── Npw
```

---

# 十、进入 SCF 之前：初始化电荷密度

现在程序需要一个初始：

[
n^{(0)}(r).
]

第一版可以非常简单：

[
n^{(0)}(r)=\frac{N_e}{\Omega}.
]

也就是均匀电子气。

于是：

```text
System
 ↓
electron_number
 ↓
volume
 ↓
initial density
 ↓
n(r)
```

以后可以升级成原子密度叠加。

---

# 十一、进入真正的 SCF

现在：

```python
scf.run()
```

开始。

我们假设第 (i) 次迭代拥有：

[
\boxed{
n^{(i)}(r)
}
]

---

# 十二、`density → hartree.py`

先把：

[
n^{(i)}(r)
]

做 FFT：

[
n^{(i)}(r)
\rightarrow
n^{(i)}(G).
]

然后：

[
V_H(G)
======

\frac{4\pi}{G^2}n(G).
]

最后 inverse FFT：

[
V_H(G)
\rightarrow
V_H(r).
]

所以：

```text
density.py
     │
     ↓
n(r)
     │
     ↓ FFT
n(G)
     │
     ↓
4π/G²
     │
     ↓
VH(G)
     │
     ↓ inverse FFT
VH(r)
```

由：

```text
hartree.py
```

完成。

---

# 十三、`density → xc.py`

同时：

[
n(r)
]

进入：

```text
xc.py
```

计算：

[
V_{xc}(r).
]

第一版采用 LDA。

所以：

```text
n(r)
 ↓
xc.py
 ↓
Vxc(r)
```

---

# 十四、`ionic_potential.py`

这个不依赖 SCF 密度。

它来自输入文件：

```yaml
potential:
    type: cosine
    V0: -1.0
```

于是：

```text
input.yaml
 ↓
io.py
 ↓
IonicPotential
 ↓
Vion(r)
```

例如：

[
V_{\rm ion}(r)
==============

V_0\sum_i\cos(G_i\cdot r).
]

---

# 十五、三种势进入 `potentials.py`

现在我们拥有：

[
V_{\rm ion}(r)
]

[
V_H(r)
]

[
V_{xc}(r).
]

进入：

```text
potentials.py
```

计算：

[
\boxed{
V_{\rm eff}(r)
==============

V_{\rm ion}(r)
+
V_H(r)
+
V_{xc}(r)
}
]

于是：

```text
          Vion(r)
             │
             │
n(r) ──→ Hartree ──→ VH(r)
 │
 └────→ XC ────────→ Vxc(r)
             │
             ↓
       potentials.py
             │
             ↓
          Veff(r)
```

---

# 十六、`Veff → hamiltonian.py`

现在终于可以构造 Kohn-Sham Hamiltonian。

[
\boxed{
H
=

-\frac12\nabla^2+V_{\rm eff}(r)
}
]

但是：

**我们仍然不构造完整的矩阵 (H_{GG'})。**

而是创建：

```python
hamiltonian = Hamiltonian(
    basis=basis,
    fft_grid=fft_grid,
    effective_potential=Veff
)
```

它提供：

```python
H.apply(C)
```

---

# 十七、`hamiltonian.py` 如何处理一个 C？

假设 eigensolver 给它：

[
C(G).
]

首先处理动能：

[
(TC)_G
======

\frac12|\mathbf k+\mathbf G|^2C_G.
]

这一步根本不需要 FFT。

然后势能部分：

### 第一步

[
C(G)
\rightarrow
\psi(r)
]

通过 FFT。

### 第二步

[
\psi(r)
\rightarrow
V_{\rm eff}(r)\psi(r).
]

### 第三步

[
V_{\rm eff}(r)\psi(r)
\rightarrow
[V_{\rm eff}\psi](G)
]

通过 FFT。

最后：

[
\boxed{
HC=TC+VC
}
]

所以：

```text
             C(G)
               │
       ┌───────┴────────┐
       ↓                ↓
     kinetic            FFT
       │                ↓
       │              ψ(r)
       │                ↓
       │          Veff(r)ψ(r)
       │                ↓
       │              FFT
       │                ↓
       └──────────→ Vψ(G)
                       │
                       ↓
                     H C
```

---

# 十八、`hamiltonian.py → eigensolver.py`

现在才轮到本征值求解器。

我们把：

```text
Hamiltonian.apply
```

交给：

```text
eigensolver.py
```

例如底层调用：

```python
scipy.sparse.linalg.eigsh
```

数学上求：

[
\boxed{
HC_n=\epsilon_nC_n
}
]

最终返回：

[
\epsilon_1,\epsilon_2,\ldots
]

以及：

[
C_1(G),C_2(G),\ldots
]

---

# 十九、这里就是你之前一直问的地方

注意：

程序并没有：

```text
H = np.array(...)
```

而是：

```text
eigensolver
      ↓
需要 HC
      ↓
调用 Hamiltonian.apply(C)
      ↓
得到 HC
```

所以整个本征值求解的数据流是：

[
\boxed{
C
\rightarrow
H(C)
\rightarrow
HC
\rightarrow
\text{iterative eigensolver}
\rightarrow
(\epsilon,C)
}
]

这正是你之前问：

> “我能不能直接调用别人的迭代本征值求解包？”

的答案。

---

# 二十、`eigensolver → occupations.py`

得到：

[
\epsilon_{nk}.
]

现在需要决定哪些状态被占据。

第一版：

> 不分自旋，每个空间轨道最多两个电子。

如果：

[
N_e=4
]

那么：

```text
ε1   occupied
ε2   occupied
ε3   empty
ε4   empty
```

即：

[
f_1=f_2=1
]

[
f_3=f_4=0.
]

进入：

```text
occupations.py
```

---

# 二十一、`C → wavefunction.py`

与此同时，本征值求解器返回：

[
C_{nk}(G).
]

进入：

```text
wavefunction.py
```

转换：

[
\boxed{
C_{nk}(G)
\rightarrow
\psi_{nk}(r)
}
]

即：

[
\psi_{nk}(r)
============

\sum_GC_{nk}(G)e^{i(k+G)\cdot r}.
]

实际上程序通过 FFT 实现。

---

# 二十二、`wavefunction + occupations → density.py`

现在我们终于可以重新计算电子密度。

公式：

[
\boxed{
n_{\rm new}(r)
==============

2\sum_{nk}
w_kf_{nk}
|\psi_{nk}(r)|^2
}
]

所以数据流：

```text
          Cnk(G)
             │
             ↓
     wavefunction.py
             │
             ↓
          ψnk(r)
             │
             ↓
     |ψnk(r)|²
             │
             │
occupations ─┤
             │
k weights ───┤
             ↓
        density.py
             │
             ↓
       n_new(r)
```

---

# 二十三、`n_new → mixing.py`

现在有两个密度：

旧的：

[
n^{(i)}(r)
]

新的：

[
n_{\rm new}(r).
]

第一版简单混合：

[
\boxed{
n^{(i+1)}=
(1-\alpha)n^{(i)}
+
\alpha n_{\rm new}
}
]

进入：

```text
mixing.py
```

得到：

[
n^{(i+1)}(r).
]

---

# 二十四、检查 SCF 是否收敛

例如检查：

[
\boxed{
\max_r
|n^{(i+1)}(r)-n^{(i)}(r)|
<10^{-6}
}
]

或者：

[
|n^{(i+1)}-n^{(i)}|<\epsilon.
]

如果：

```text
没有收敛
```

那么：

[
n^{(i+1)}
]

重新进入：

```text
hartree.py
xc.py
potentials.py
hamiltonian.py
eigensolver.py
...
```

于是形成：

[
\boxed{
n^{(i)}
\rightarrow
V_{\rm eff}[n^{(i)}]
\rightarrow
H[n^{(i)}]
\rightarrow
\psi^{(i)}
\rightarrow
n_{\rm new}^{(i)}
\rightarrow
n^{(i+1)}
}
]

---

# 二十五、这就是 SCF 的完整循环

可以把整个循环压缩成：

```text
                  n_old
                    │
          ┌─────────┴─────────┐
          ↓                   ↓
      Hartree                XC
          │                   │
          ↓                   ↓
         VH                  Vxc
          │                   │
          └─────────┬─────────┘
                    ↓
                  Veff
                    ↑
                    │
                + Vion
                    │
                    ↓
                    H
                    │
                    ↓
                  H(C)
                    │
                    ↓
              eigensolver
                    │
             ┌──────┴──────┐
             ↓             ↓
          ε_nk          C_nk(G)
             │             │
             ↓             ↓
      occupations    wavefunction
                           │
                           ↓
                         ψ(r)
                           │
                           ↓
                        density
                           │
                           ↓
                        n_new
                           │
                           ↓
                         mixing
                           │
                           ↓
                         n_old
                           │
                           └──────────────→ 下一轮
```

---

# 二十六、SCF 收敛以后发生什么？

假设第 27 次：

[
n^{(27)}
]

满足收敛条件。

这时候 SCF 不再继续。

我们已经有：

[
\boxed{
n(r)
}
]

[
\boxed{
\epsilon_{nk}
}
]

[
\boxed{
C_{nk}(G)
}
]

[
\boxed{
\psi_{nk}(r)
}
]

---

# 二十七、进入 `energy.py`

现在计算最终总能量。

包括我们设计中的：

[
T_s
]

[
E_H
]

[
E_{xc}
]

[
E_{\rm ion}
]

以及相应的离子-离子能等。

最终得到：

[
\boxed{
E_{\rm total}
}
]

---

# 二十八、进入 `dos.py`

已有：

[
\epsilon_{nk}.
]

构造：

[
D(E)
====

\sum_{nk}
w_k\delta(E-\epsilon_{nk}).
]

实际用 Gaussian 展宽：

[
\delta(E-\epsilon)
\rightarrow
\frac{1}{\sigma\sqrt{2\pi}}
e^{-(E-\epsilon)^2/(2\sigma^2)}.
]

得到：

```text
energy grid
DOS
```

---

# 二十九、最后 `io.py` 输出

最终输出的数据至少可以有：

```text
output/
│
├── scf.log
├── eigenvalues.dat
├── occupations.dat
├── density.dat
├── wavefunctions.dat
├── total_energy.dat
└── dos.dat
```

---

## `scf.log`

例如：

```text
SCF iteration    Energy           Density error

1                ...
2                ...
3                ...
...
27               -...
```

---

## `eigenvalues.dat`

保存：

[
\epsilon_{nk}.
]

例如：

```text
kpoint  band  energy
0       1     -1.234
0       2      0.567
0       3      2.341
```

---

## `occupations.dat`

保存：

[
f_{nk}.
]

---

## `density.dat`

保存：

[
n(r).
]

---

## `wavefunctions.dat`

如果需要保存：

[
C_{nk}(G)
]

或者：

[
\psi_{nk}(r).
]

第一版我反而建议主要保存：

[
C_{nk}(G)
]

因为它是平面波基组下真正的本征向量。

---

## `total_energy.dat`

保存：

[
E_{\rm total}.
]

---

## `dos.dat`

保存：

[
E,D(E).
]

---

# 三十、把整个程序压缩成“一条数据管道”

如果你以后忘记所有细节，只需要记住这一张：

```text
INPUT FILE
    │
    ↓
   io.py
    │
    ↓
 System
    │
    ├────────→ lattice.py
    │               │
    │               ↓
    │          A, B, volume
    │
    ↓
 basis.py
    │
    ↓
 {G}, k+G, T_G, Npw
    │
    ↓
 fft_grid.py
    │
    ↓
 FFT mappings
    │
    ↓
 initial density n⁰(r)
    │
    ╔══════════════════════════════════╗
    ║              SCF                 ║
    ║                                  ║
    ║  n(r)                            ║
    ║   │                              ║
    ║   ├──→ hartree.py → VH(r)        ║
    ║   │                              ║
    ║   └──→ xc.py → Vxc(r)            ║
    ║                                  ║
    ║  Vion(r) ← ionic_potential.py    ║
    ║       │                           ║
    ║       ↓                           ║
    ║  potentials.py                   ║
    ║       │                           ║
    ║       ↓                           ║
    ║  Veff(r)                         ║
    ║       │                           ║
    ║       ↓                           ║
    ║  hamiltonian.py                  ║
    ║       │                           ║
    ║       ↓                           ║
    ║  C → H(C)                        ║
    ║       │                           ║
    ║       ↓                           ║
    ║  eigensolver.py                  ║
    ║       │                           ║
    ║       ↓                           ║
    ║  εnk, Cnk(G)                     ║
    ║       │                           ║
    ║       ├──→ occupations.py        ║
    ║       │                           ║
    ║       └──→ wavefunction.py       ║
    ║                    │             ║
    ║                    ↓             ║
    ║                  ψnk(r)          ║
    ║                    │             ║
    ║                    ↓             ║
    ║                density.py        ║
    ║                    │             ║
    ║                    ↓             ║
    ║                  n_new           ║
    ║                    │             ║
    ║                    ↓             ║
    ║                 mixing.py        ║
    ║                    │             ║
    ║                    ↓             ║
    ║                  n_next ─────────┘
    ║                                  ║
    ╚══════════════════════════════════╝
                    │
              SCF converged
                    │
          ┌─────────┴──────────┐
          ↓                    ↓
      energy.py             dos.py
          ↓                    ↓
     E_total                 DOS(E)
          │                    │
          └─────────┬──────────┘
                    ↓
                  io.py
                    ↓
              OUTPUT FILES
```

---

# 三十一、最关键的数据对象其实只有 10 个

以后写程序的时候，你可以把整个项目理解成下面这些数据在不同 `.py` 之间传递：

| 数据                 | 数学含义                    | 主要产生者             | 主要使用者                     |            |               |
| ------------------ | ----------------------- | ----------------- | ------------------------- | ---------- | ------------- |
| `Lattice`          | (A,B,\Omega)            | `lattice.py`      | `basis.py`, `fft_grid.py` |            |               |
| `G_vectors`        | (\mathbf G)             | `basis.py`        | `Hamiltonian`, FFT        |            |               |
| `kinetic_energies` | (\frac12                | k+G               | ^2)                       | `basis.py` | `Hamiltonian` |
| `FFTGrid`          | (G\leftrightarrow r) 映射 | `fft_grid.py`     | 多个模块                      |            |               |
| `density`          | (n(r))                  | `density.py`      | Hartree/XC                |            |               |
| `Veff`             | (V_{\rm eff}(r))        | `potentials.py`   | Hamiltonian               |            |               |
| `C`                | (C_{nk}(G))             | `eigensolver.py`  | wavefunction/density      |            |               |
| `eigenvalues`      | (\epsilon_{nk})         | `eigensolver.py`  | occupations/energy/DOS    |            |               |
| `wavefunctions`    | (\psi_{nk}(r))          | `wavefunction.py` | density                   |            |               |
| `DOS`              | (D(E))                  | `dos.py`          | 输出                        |            |               |

---

## 最后一个非常重要的架构原则

**数据流不是“每个 py 调下一个 py”。**

例如：

```text
density.py → hartree.py → xc.py → potentials.py
```

并不是说 `density.py` 主动 import 然后调用所有东西。

真正应该是：

```text
scf.py
   │
   ├── 调 density
   ├── 调 hartree
   ├── 调 xc
   ├── 调 potentials
   ├── 调 Hamiltonian
   ├── 调 eigensolver
   ├── 调 occupations
   ├── 调 density
   └── 调 mixing
```

也就是说：

[
\boxed{\texttt{scf.py 是调度者}}
]

而各个 `.py` 是：

[
\boxed{\text{专业工人}}
]

它们各自完成一个明确的数学变换。

这会让你以后非常容易把某个模块替换掉，例如把 `eigsh` 换成 Davidson，而不用动整个 DFT 程序。
